import { Container, Typography } from "@mui/material";
import React from "react";

const page = () => {
  return (
    <Container>
      <Typography>Hello</Typography>
    </Container>
  );
};

export default page;
